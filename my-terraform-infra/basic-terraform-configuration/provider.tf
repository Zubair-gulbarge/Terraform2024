provider "aws" {
    region = "us-east-1" # Set your desired AWS region
    # access_key = var.access_key # I havn't declared any variables yet so that's why it's showing error.
    # secret_key = var.secret_key
}